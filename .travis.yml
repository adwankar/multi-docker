sudo: required
services:
  - docker

before_install:
  - docker build -t adwankar/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run adwankar/react-test npm test -- --coverage

after_success:
  - docker build -t adwankar/multi-client ./client
  - docker build -t adwankar/multi-nginx ./nginx
  - docker build -t adwankar/multi-server ./server
  - docker build -t adwankar/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push adwankar/multi-client
  - docker push adwankar/multi-nginx
  - docker push adwankar/multi-server
  - docker push adwankar/multi-worker
